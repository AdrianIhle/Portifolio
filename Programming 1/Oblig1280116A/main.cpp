#include <iostream>
#include "board.h"
#include "player.h"

using namespace std;

int main()
{
Board mBoard;
Player mPlayer;

  mBoard.putCharacter(mPlayer.getSprite(), mPlayer.position.getX(), mPlayer.position.getY());


}


